####################################

Round 3:
- The stats section in the overview page needs some extra gap between the title and the stats cards.
- The "add announcement" button needs to be outline; to be more visible.
- Creating a class stil fails.
- Update importing student to accept ".ods" and any other excel sheets fromat for Linux, Mac, and Widows.
- The content of the search bar is still not align well.
- Navigation issue still there:
1- ## Error Type
Console Error

## Error Message
Maximum update depth exceeded. This can happen when a component calls setState inside useEffect, but useEffect either doesn't have a dependency array, or one of the dependencies changes on every render.


    at useFormState.useCallback[setData] [as setData] (src/lib/hooks/useFormState.ts:53:5)
    at SettingsPage.useEffect (src/app/[lang]/dashboard/admin/settings/page.tsx:129:15)

## Code Frame
  51 |
  52 |   const setData = useCallback((data: Partial<T>) => {
> 53 |     setState((prev) => ({
     |     ^
  54 |       ...prev,
  55 |       data: { ...prev.data, ...data },
  56 |     }));

Next.js version: 16.2.1 (Turbopack)
2- ## Error Type
Console Error

## Error Message
Maximum update depth exceeded. This can happen when a component calls setState inside useEffect, but useEffect either doesn't have a dependency array, or one of the dependencies changes on every render.


    at a (<anonymous>:null:null)
    at <unknown> (src/app/[lang]/dashboard/admin/layout.tsx:51:13)
    at Array.map (<anonymous>:null:null)
    at AdminLayout (src/app/[lang]/dashboard/admin/layout.tsx:46:19)

## Code Frame
  49 |             : pathname.startsWith(href);
  50 |           return (
> 51 |             <Link
     |             ^
  52 |               key={key}
  53 |               href={href}
  54 |               aria-label={t[key]}

Next.js version: 16.2.1 (Turbopack)


################################################################


Round 4:
- Edit the name of the school still fails, I can't write anything in the text fields, even though the field state tells it's editable (fix in round 3, thank you).
- ## Error Type
Console Error

## Error Message
Maximum update depth exceeded. This can happen when a component calls setState inside useEffect, but useEffect either doesn't have a dependency array, or one of the dependencies changes on every render.


    at useFormState.useCallback[setData] [as setData] (src/lib/hooks/useFormState.ts:53:5)
    at SettingsPage.useEffect (src/app/[lang]/dashboard/admin/settings/page.tsx:129:15)

## Code Frame
  51 |
  52 |   const setData = useCallback((data: Partial<T>) => {
> 53 |     setState((prev) => ({
     |     ^
  54 |       ...prev,
  55 |       data: { ...prev.data, ...data },
  56 |     }));

Next.js version: 16.2.1 (Turbopack)


################################################################


Round 5:
- There is a student visible in the students list, please remove them.
- I still can't import .ods files yet.
- The gap I requested still not visible.
- Exam types must be {ENG: (1st month, 2nd month, 3rd month, mid term, final), ARABIC: (شهر أول، شهر ثاني، شهر ثالث،، منتصف الفصل، نهائي)}.


################################################################


Round 6:
- there are no students visible after successfully importing them.
	- when import is successful there should be a step to create an email for each student.
	- and to set the rest of the field for the student recored.
	- the plan for that:
		- after uploading the file, the user will see the student list as confirmation (exactly as it's done currently).
		- next, the user must see form for setting and email english name, and password for each student.
		- next, the user must be able to set the class and section of the uploaded file/students list.
		- next, when successful, the user will be able to see the list of the students added to the system as if they were added one by one.


#################################################################


Round 7:
- Updating an exam fails:
{
## Error Type
Console ClientResponseError 400

## Error Message
Failed to update record.


    at async handleSubmitExam (src/app/[lang]/dashboard/admin/subjects_exams/page.tsx:283:9)

## Code Frame
  281 |
  282 |       if (examListCrudState.state.editingId) {
> 283 |         await pb.collection("exam_schedules").update(examListCrudState.state.editingId, data);
      |         ^
  284 |       } else {
  285 |         await pb.collection("exam_schedules").create(data);
  286 |       }

Next.js version: 16.2.1 (Turbopack)

}


###################################################################


Round 8:
- the import button text should be {ENG: "import students list", ARABIC: "تحميل كشف الأسماء"}.
- the email should be in this format: first.last@manakher.edu.jo
- the import wizard has a small font, make it bigger.
- the imported students still not shown on the page after importing them successfully.


###################################################################


Round 9:
- the exam updating still fail.
- ## Error Type
Console ClientResponseError 400

## Error Message
Failed to update record.


    at async handleSubmitExam (src/app/[lang]/dashboard/admin/subjects_exams/page.tsx:283:9)

## Code Frame
  281 |
  282 |       if (examListCrudState.state.editingId) {
> 283 |         await pb.collection("exam_schedules").update(examListCrudState.state.editingId, data);
      |         ^
  284 |       } else {
  285 |         await pb.collection("exam_schedules").create(data);
  286 |       }

Next.js version: 16.2.1 (Turbopack)

